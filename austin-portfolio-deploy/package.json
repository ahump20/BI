{
  "name": "blaze-intelligence",
  "version": "1.0.0",
  "description": "Blaze Intelligence - The Deep South Sports Authority",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "start": "npx live-server --port=8000 --entry-file=index.html",
    "dev": "npx live-server --port=8000 --entry-file=index.html",
    "deploy": "echo 'Deploying to production...'",
    "test": "echo 'Running tests...'"
  }
}
